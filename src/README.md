# Zig core
`main.zig` connects the modules and exports a small WASM ABI. Use a 30 Hz fixed simulation step, bounded frame catch-up, and renderer-independent state. Keep browser-specific code outside this folder.
